import foo.bar version a0 as ver1;

function mainVer1 (string[] args) (string greeting) {
    var name = ver1:baz();
    var salutation = args[0];
    greeting = salutation + " " + name;
    return;
}
